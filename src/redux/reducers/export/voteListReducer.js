// import { VOTE_LIST, VOTE_LIST_SUCCESS, VOTE_LIST_ERROR } from '../../Type';

const INITIAL_STATE = {
  list: [],
};

const listReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default listReducer;
